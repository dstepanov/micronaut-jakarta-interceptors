package io.micronaut.interceptor.test.state;

/** Produced by {@link ProducingFactory}, which is what makes Micronaut proxy it with a separate target. */
public class ProducedService {

    public String work() {
        return "done";
    }
}
